<UserControl x:Class="VideoAutoTool.App.Controls.DesignCanvasControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="#FF0E0E0E">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Zoom Controls -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="5">
            <ComboBox x:Name="ZoomComboBox" Width="100" SelectedIndex="1">
                <ComboBoxItem>50%</ComboBoxItem>
                <ComboBoxItem>Vừa khung</ComboBoxItem>
                <ComboBoxItem>100%</ComboBoxItem>
            </ComboBox>
        </StackPanel>

        <!-- Canvas Area with ScrollViewer -->
        <ScrollViewer Grid.Row="1" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
            <Border Background="#FF1A1A1A" Padding="50">
                <Canvas x:Name="MainCanvas" 
                        Width="1280" 
                        Height="720" 
                        Background="#FF2D2D30"
                        ClipToBounds="True">
                    <!-- Layers will be rendered here dynamically -->
                </Canvas>
            </Border>
        </ScrollViewer>
    </Grid>
</UserControl>
