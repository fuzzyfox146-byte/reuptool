FFmpeg binaries (ffmpeg.exe, ffprobe.exe) are downloaded by scripts/fetch-ffmpeg.ps1
into this folder and copied next to the app as tools/ffmpeg/.
They are git-ignored because the essentials build is large.
